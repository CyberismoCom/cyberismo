== Parameters

* `Skill`: the name of a skill
* `Tool`: the name of an MCP tool that the skill uses, such as `"get_card"`

== Description

A skill declares the MCP tools that its instructions refer to. There is a separate `skillRelatedTool` term for each tool of the skill.
