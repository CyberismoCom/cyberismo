[.lead]
The complete specification: every resource you can configure, the query language, the logic programming predicates, and the external functions.

This section is for looking things up rather than reading through. If you are new to Answer Set Programming, read {{#xref}}"cardKey":"docs_28"{{/xref}} in {{#xref}}"cardKey":"docs_is3958yg"{{/xref}} first, since the reference material below assumes it.

*Configuring resources* covers workflows, card types, field types, link types, templates, reports, graph models and calculations.

*Queries, reasoning, and visualisations* covers how cards map to a logic program, the query language, and the predicates behind reports, graphs, calculated fields, policy checks, notifications and connectors.

*Naming conventions* covers how resources are named and referenced.

== In this section

{{#report}}
  "name": "base/reports/childrenList"
{{/report}}
