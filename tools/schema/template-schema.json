{
    "title": "Template",
    "$id": "template-schema",
    "description": "A template object provides supplemental information about a template directory structure ",
    "type": "object",
    "properties": {
        "buttonLabel": {
            "description": "The label of the button to create from this template. For example, 'Create a New Decision'.",
            "type": "string"
        },
        "namePrompt": {
            "description": "How the name of the new card or card hierarchy should be prompted. For example, 'The title of the new decision:'.",
            "type": "string"
        }
    },
    "required": [
        "buttonLabel",
        "namePrompt"
    ]
}